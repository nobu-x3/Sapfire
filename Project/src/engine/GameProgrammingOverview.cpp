#include "Game.h"
#include "engine/Test.h"

int main(int argc, char *argv[])
{
	TestApp testApp;
	testApp.Tick();
	// while (true)
	// 	;
	/* Game game; */
	/* bool success = game.Initialize(); */
	/* if (success) */
	/* 	game.Update(); */
	/* game.Shutdown(); */
	return 0;
}
