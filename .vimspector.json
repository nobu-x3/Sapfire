{
  "configurations": {
    "launch": {
      "adapter": "CodeLLDB",
      "filetypes": [ "zig" ],
      "configuration": {
        "request": "launch",
        "program": "${workspaceRoot}/zig-out/bin/Sapfire",
        "cwd": "${workspaceRoot}/"
      }
    }
  }
}
